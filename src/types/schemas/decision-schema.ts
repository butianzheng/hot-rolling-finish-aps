// ==========================================
// 决策层 Zod Schema 验证模式（兼容导出）
// ==========================================
// 注意：真实定义已迁移到 `src/api/ipcSchemas/decision.ts`，这里仅做 re-export 以兼容旧引用路径。
// 推荐：业务代码优先从 `src/api/ipcSchemas.ts` 或 `src/api/ipcSchemas/decision.ts` 引用。

export * from '../../api/ipcSchemas/decision';

